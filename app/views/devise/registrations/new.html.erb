<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name),
:html => {:class => 'form-signup', :role => 'form'}) do |f| %>

<% if devise_error_messages! != "" %>
<div class="alert alert-danger alert-dismissable">
  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">
    &times;
  </button>
  <%= devise_error_messages! %>
</div>
<% end %>

<h2 class="form-signin-heading">Crea tu cuenta</h2>
<input type="text" name="institucion" autofocus="true" class="form-control" placeholder="Institución"/>
<%= f.email_field :email, :class => 'form-control', :placeholder => 'Email' %>
<%= f.password_field :password, :class => 'form-control', :placeholder => 'Password' %>
<%= f.password_field :password_confirmation, :class => 'form-control', :placeholder => 'Confirmar password' %>
</br>
<button class="btn btn-lg btn-primary btn-block" type="submit">
  Registrarse
</button>
<% end %>
