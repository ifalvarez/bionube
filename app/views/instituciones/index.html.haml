.container
  = link_to new_institucion_path, :class => "btn btn-default pull-right anadir" do
    %span.glyphicon.glyphicon-plus
    Añadir
  %table.manasyPanel.table
    - @instituciones.each do |institucion|
      %tr
        %td= institucion.nombre
        %td
          = link_to edit_institucion_path(institucion) do
            %span.glyphicon.glyphicon-edit
        %td
          = link_to institucion, :method => :delete, :data => { :confirm => 'Esta seguro?' } do
            %span.glyphicon.glyphicon-trash
