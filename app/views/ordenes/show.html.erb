<div class="container">
  <%= link_to edit_orden_path(@orden), :class => "btn btn-default pull-right anadir" do %>
    <span class="glyphicon glyphicon-edit"></span> Editar
  <% end %>
  <%= render partial: "show", object: @orden, as: "orden" %>
</div>